import java.awt.Color;

/**
 * Write a description of class PerPixelFilter here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public interface PerPixelFilter
{
    public Color applyFilter(Color source);
}
